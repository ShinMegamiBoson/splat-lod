# PC-main experiment, September 6

Completed T001: three proposals, four frozen inputs, seven modes, two reversed-order trials. All 56 cases completed and all 280 image comparisons were independently checked.

None of P1/P2/P3 passes the all-input adoption rule. Exact/visible remains the default. Contribution culling is useful on the shop and 16M city input but fails quality on the bee and 32M input. All alternatives are explicit options.

After the run, added three fresh ideas and nine components: selected-ID SH caching, coverage-preserving tiny-splat aggregation, and physical bank locality with permanent ID remapping. Six new combinations were resolved into two proposals and four explicit rejections. Ranked frontier: P5 (aggregation with locality ablations), then P4 (selected-ID SH cache). These are untested, not validated performance improvements. P1/P2/P3 left the promotion frontier.

The authorized one-batch budget is complete. No test batch is open and no further tests are running. A separate experiment would implement one of the new proposals, audit its invariants, freeze its controls and then measure it. Remaining unknowns include other devices, longer paths, memory/streaming performance and all-angle quality.
